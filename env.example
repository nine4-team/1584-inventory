# Frontend (Vite)
# These are the only vars the local React app needs.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional feature flag
VITE_ENABLE_QR=false

# Notes:
# - Service-role keys and HighLevel webhook secrets live in Supabase env config.
# - Update this file (and your private `.env`) only when local client vars change.

